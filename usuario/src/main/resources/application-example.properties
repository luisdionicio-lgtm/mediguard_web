
# ----------------------------------------------------------------------------------
#spring.application.name=usuario
#server.port=8081
# ----------------------------------------------------------------------------------
# Runtime database configuration. Provide real values via environment or deployment secrets.
#DB_URL=jdbc:postgresql://localhost:5432/mediguard_db
#DB_USERNAME=mediguard_user
#DB_PASSWORD=replace-with-secure-password
# ----------------------------------------------------------------------------------
#spring.datasource.url=${DB_URL}
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}
#spring.datasource.driver-class-name=org.postgresql.Driver
# ----------------------------------------------------------------------------------
#spring.jpa.hibernate.ddl-auto=none

# Proveedor de notificaciones SOS. NoOp es el valor seguro por defecto.
#app.notifications.provider=noop
#spring.jpa.show-sql=false
#spring.jpa.properties.hibernate.format_sql=false
# ----------------------------------------------------------------------------------
# JWT configuration. Do not commit real values.
# JWT_SECRET must be at least 32 characters long and should be randomly generated.
# ----------------------------------------------------------------------------------
#app.security.jwt.secret=${JWT_SECRET}
#app.security.jwt.access-token-seconds=${JWT_ACCESS_TOKEN_SECONDS:3600}
#app.security.jwt.refresh-token-seconds=${JWT_REFRESH_TOKEN_SECONDS:604800}
# ----------------------------------------------------------------------------------
# Correo para recuperar contraseña. MAIL_PASSWORD es una "Contraseña de aplicación"
# de Gmail (no la contraseña real de la cuenta). Nunca commitear estos valores.
# ----------------------------------------------------------------------------------
#spring.mail.host=smtp.gmail.com
#spring.mail.port=587
#spring.mail.username=${MAIL_USERNAME}
#spring.mail.password=${MAIL_PASSWORD}
#spring.mail.properties.mail.smtp.auth=true
#spring.mail.properties.mail.smtp.starttls.enable=true
#app.mail.from=${MAIL_USERNAME}
#app.frontend.reset-password-url=http://localhost:5173/reset-password
# ----------------------------------------------------------------------------------
# Google OAuth (login social). Do not commit real values.
# ----------------------------------------------------------------------------------
#spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
#spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
#spring.security.oauth2.client.registration.google.scope=email,profile
